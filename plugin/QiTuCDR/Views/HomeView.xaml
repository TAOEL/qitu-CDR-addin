<UserControl x:Class="QiTuCDR.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/QiTuCDR;component/Resources/DesignSystem.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{StaticResource PageContainerStyle}">
        <Grid ClipToBounds="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource PageTitlebarStyle}">
                <TextBlock Text="工具面板"
                           Style="{StaticResource TitlebarTitleStyle}"
                           VerticalAlignment="Center" />
            </Border>

            <Border Grid.Row="1" Style="{StaticResource PageContentStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource PageTitleStyle}"
                               Text="工具面板" Margin="0,0,0,12" />
                    <TextBlock Style="{StaticResource PageSubtitleStyle}"
                               Text="欢迎使用 QiTuCDR 企图插件。请从上方工具栏选择功能分类。"
                               Margin="0,0,0,16" />

                    <TextBlock Text="常用工具"
                               FontFamily="{StaticResource PrimaryFont}"
                               FontSize="{StaticResource FontSizeMd}"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               Margin="0,0,0,8" />
                    <TextBlock Style="{StaticResource PlaceholderTextStyle}"
                               Text="暂无常用工具，使用后自动记录。" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
