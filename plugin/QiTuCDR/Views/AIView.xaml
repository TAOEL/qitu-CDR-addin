<UserControl x:Class="QiTuCDR.Views.AIView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/QiTuCDR;component/Resources/DesignSystem.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{StaticResource PageContainerStyle}">
        <Grid ClipToBounds="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource PageTitlebarStyle}">
                <TextBlock Text="AI 助手"
                           Style="{StaticResource TitlebarTitleStyle}"
                           VerticalAlignment="Center" />
            </Border>

            <Border Grid.Row="1" Style="{StaticResource PageContentStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource PageTitleStyle}" Text="AI 助手" Margin="0,0,0,12" />
                    <TextBlock Style="{StaticResource PageSubtitleStyle}" Text="AI 智能辅助：文案改写、排版建议、色彩推荐。" Margin="0,0,0,16" />
                    <TextBlock Style="{StaticResource PlaceholderTextStyle}" Text="功能开发中..." />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
