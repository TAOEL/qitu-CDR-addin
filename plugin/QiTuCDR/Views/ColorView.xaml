<UserControl x:Class="QiTuCDR.Views.ColorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/QiTuCDR;component/Resources/DesignSystem.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{StaticResource PageContainerStyle}">
        <Grid ClipToBounds="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource PageTitlebarStyle}">
                <TextBlock Text="颜色工具"
                           Style="{StaticResource TitlebarTitleStyle}"
                           VerticalAlignment="Center" />
            </Border>

            <Border Grid.Row="1" Style="{StaticResource PageContentStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource PageTitleStyle}" Text="颜色工具" Margin="0,0,0,12" />
                    <TextBlock Style="{StaticResource PageSubtitleStyle}" Text="色板管理、颜色批量替换、色彩分析等颜色处理功能。" Margin="0,0,0,16" />
                    <TextBlock Style="{StaticResource PlaceholderTextStyle}" Text="功能开发中..." />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
